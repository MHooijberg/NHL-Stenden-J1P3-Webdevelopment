@page "{uid?}"
@model MyMeals.Pages.ProfielModel
@{
    ViewData["Title"] = "Profiel";
}

<div class="container">
    <div style="border: 3px ridge;" class="col-6 p-3 rounded">
        @if (@*User.Identity.IsAuthenticated && User.Identity.Name == null*@ false)
        {
            <button class="btn btn-success">Aanpassen</button>
        }
        <div class="row">
            <div class="col-4">
                <img class="mx-auto d-block" style="width: 100px; border: 6px solid black; border-radius: 50%;" src="~/img/pfp/ProfilePictureTemplate.png"/>
            </div>
            <div class="col-8 align-self-center">
                <h1>@Model.UserName</h1>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <ul class="list-unstyled ml-4" style="font-size: larger">
                    <li><i class="p-2 text-success fas fa-map-marker-alt"></i>@Model.Location</li>
                    <li><i class="p-2 text-success far fa-envelope"></i>@Model.Email</li>
                    <li><i class="p-2 text-success fas fa-phone-alt"></i>@Model.Phone</li>
                </ul>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <h3>Over Gebruiker</h3>
                <p>@Model.About</p>
            </div>
        </div>
    </div>
    <div>
        <!-- TODO: Recommended Meals -->
    </div>
</div>